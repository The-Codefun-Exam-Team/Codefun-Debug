import type { Metadata } from "next";

export const generateStaticParams = () => [];

export const metadata: Metadata = {
  title: "Submissions",
};

const Page = () => <div>Submissions page</div>;

export default Page;
