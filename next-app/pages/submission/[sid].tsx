import type { NextPage } from "next";

/**
 * 
 * @returns Submission page
 * @url /submission/[sid]
 */
const Submission: NextPage = () => {
	return (
		<>
			<div>Submission page</div>
		</>
	);
};

export default Submission;
